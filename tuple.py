# ---------------------------------------------------------------------
# 元组
tu = ('a', 1, 'b', 'Hello')

# ---------------------------------------------------------------------
# 拆包
c1, x, c2, s = tu
print(tu)
print(c1, x, c2, s)

# ---------------------------------------------------------------------
# 不可变的数据结构
tu[0] = 3  # 这一句会抛出异常